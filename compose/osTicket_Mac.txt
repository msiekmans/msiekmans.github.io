services:
  osticket:
    image: ppcm/osticket-server:latest
    container_name: OsTicket
    depends_on:
      - osticket_db
    ports:
      - "8080:80"  # Maps host port 8080 to container port 80   
    networks:
 #     local-network:
 #       ipv4_address: 192.168.139.15
      backend-network:
    environment:
      # Database configuration
      MYSQL_HOST: osticket_db
      MYSQL_PORT: 3306
      MYSQL_DATABASE: my_database_name
      MYSQL_USER: some_user
      MYSQL_PASSWORD: some_password

      # Admin configuration
      INSTALL_EMAIL: yourown@email
      INSTALL_URL: http://osticket.local
      INSTALL_NAME: Helpdesk
      INSTALL_SECRET: mysecret
    volumes:
      - CHANGE_TO_COMPOSE_DATA_PATH/osticket/config:/config
    restart: unless-stopped
    
  osticket_db:
    image: mariadb:11.3-jammy
    container_name: OsTicket_DB
    security_opt:
      - no-new-privileges:true
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_USER: some_user
      MYSQL_PASSWORD: some_password
      MYSQL_DATABASE: my_database_name
    volumes:
      - CHANGE_TO_COMPOSE_DATA_PATH/osticket/db:/var/lib/mysql:rw
    networks:
      - backend-network
    restart: unless-stopped

networks:
  backend-network:
    name: osticket-backend-network
#  local-network:
#    external: true
